{
  "name": "maddie-assets",
  "version": "1.0.0",
  "description": "Repositório público de assets da Maddie — perguntas de quiz, contribuições abertas.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "check:duplicates": "node scripts/check-duplicates.mjs",
    "lint": "prettier --check \"questions/**/*.json\"",
    "lint:fix": "prettier --write \"questions/**/*.json\""
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "prettier": "^3.3.3"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "chalk": "^5.6.2",
    "ora": "^9.4.0"
  }
}
